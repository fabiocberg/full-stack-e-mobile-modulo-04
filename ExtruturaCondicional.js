// Extrutura condicional
const numero = Math.random() * 100;
if (numero >= 50) {
  console.log("Número maior do que 50: " + Math.round(numero));
} else {
  console.log("Número menor do que 50: " + Math.round(numero));
}
